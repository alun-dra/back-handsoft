// Code generated by ent, DO NOT EDIT.

package ent

import (
	"back/internal/ent/shift"
	"back/internal/ent/shiftinstance"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ShiftInstance is the model entity for the ShiftInstance schema.
type ShiftInstance struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// ShiftID holds the value of the "shift_id" field.
	ShiftID int `json:"shift_id,omitempty"`
	// Date holds the value of the "date" field.
	Date time.Time `json:"date,omitempty"`
	// State holds the value of the "state" field.
	State string `json:"state,omitempty"`
	// Mode holds the value of the "mode" field.
	Mode string `json:"mode,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShiftInstanceQuery when eager-loading is set.
	Edges        ShiftInstanceEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ShiftInstanceEdges holds the relations/edges for other nodes in the graph.
type ShiftInstanceEdges struct {
	// Shift holds the value of the shift edge.
	Shift *Shift `json:"shift,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ShiftOrErr returns the Shift value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShiftInstanceEdges) ShiftOrErr() (*Shift, error) {
	if e.Shift != nil {
		return e.Shift, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: shift.Label}
	}
	return nil, &NotLoadedError{edge: "shift"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShiftInstance) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case shiftinstance.FieldID, shiftinstance.FieldShiftID:
			values[i] = new(sql.NullInt64)
		case shiftinstance.FieldState, shiftinstance.FieldMode:
			values[i] = new(sql.NullString)
		case shiftinstance.FieldDate, shiftinstance.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShiftInstance fields.
func (_m *ShiftInstance) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case shiftinstance.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case shiftinstance.FieldShiftID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field shift_id", values[i])
			} else if value.Valid {
				_m.ShiftID = int(value.Int64)
			}
		case shiftinstance.FieldDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field date", values[i])
			} else if value.Valid {
				_m.Date = value.Time
			}
		case shiftinstance.FieldState:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field state", values[i])
			} else if value.Valid {
				_m.State = value.String
			}
		case shiftinstance.FieldMode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field mode", values[i])
			} else if value.Valid {
				_m.Mode = value.String
			}
		case shiftinstance.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ShiftInstance.
// This includes values selected through modifiers, order, etc.
func (_m *ShiftInstance) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryShift queries the "shift" edge of the ShiftInstance entity.
func (_m *ShiftInstance) QueryShift() *ShiftQuery {
	return NewShiftInstanceClient(_m.config).QueryShift(_m)
}

// Update returns a builder for updating this ShiftInstance.
// Note that you need to call ShiftInstance.Unwrap() before calling this method if this ShiftInstance
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ShiftInstance) Update() *ShiftInstanceUpdateOne {
	return NewShiftInstanceClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ShiftInstance entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ShiftInstance) Unwrap() *ShiftInstance {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ShiftInstance is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ShiftInstance) String() string {
	var builder strings.Builder
	builder.WriteString("ShiftInstance(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("shift_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ShiftID))
	builder.WriteString(", ")
	builder.WriteString("date=")
	builder.WriteString(_m.Date.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("state=")
	builder.WriteString(_m.State)
	builder.WriteString(", ")
	builder.WriteString("mode=")
	builder.WriteString(_m.Mode)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ShiftInstances is a parsable slice of ShiftInstance.
type ShiftInstances []*ShiftInstance
