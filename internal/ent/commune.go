// Code generated by ent, DO NOT EDIT.

package ent

import (
	"back/internal/ent/city"
	"back/internal/ent/commune"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Commune is the model entity for the Commune schema.
type Commune struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CommuneQuery when eager-loading is set.
	Edges         CommuneEdges `json:"edges"`
	city_communes *int
	selectValues  sql.SelectValues
}

// CommuneEdges holds the relations/edges for other nodes in the graph.
type CommuneEdges struct {
	// City holds the value of the city edge.
	City *City `json:"city,omitempty"`
	// Addresses holds the value of the addresses edge.
	Addresses []*Address `json:"addresses,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// CityOrErr returns the City value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CommuneEdges) CityOrErr() (*City, error) {
	if e.City != nil {
		return e.City, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: city.Label}
	}
	return nil, &NotLoadedError{edge: "city"}
}

// AddressesOrErr returns the Addresses value or an error if the edge
// was not loaded in eager-loading.
func (e CommuneEdges) AddressesOrErr() ([]*Address, error) {
	if e.loadedTypes[1] {
		return e.Addresses, nil
	}
	return nil, &NotLoadedError{edge: "addresses"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Commune) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case commune.FieldID:
			values[i] = new(sql.NullInt64)
		case commune.FieldName:
			values[i] = new(sql.NullString)
		case commune.FieldCreatedAt, commune.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case commune.ForeignKeys[0]: // city_communes
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Commune fields.
func (_m *Commune) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case commune.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case commune.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case commune.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case commune.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case commune.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field city_communes", value)
			} else if value.Valid {
				_m.city_communes = new(int)
				*_m.city_communes = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Commune.
// This includes values selected through modifiers, order, etc.
func (_m *Commune) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCity queries the "city" edge of the Commune entity.
func (_m *Commune) QueryCity() *CityQuery {
	return NewCommuneClient(_m.config).QueryCity(_m)
}

// QueryAddresses queries the "addresses" edge of the Commune entity.
func (_m *Commune) QueryAddresses() *AddressQuery {
	return NewCommuneClient(_m.config).QueryAddresses(_m)
}

// Update returns a builder for updating this Commune.
// Note that you need to call Commune.Unwrap() before calling this method if this Commune
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Commune) Update() *CommuneUpdateOne {
	return NewCommuneClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Commune entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Commune) Unwrap() *Commune {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Commune is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Commune) String() string {
	var builder strings.Builder
	builder.WriteString("Commune(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Communes is a parsable slice of Commune.
type Communes []*Commune
